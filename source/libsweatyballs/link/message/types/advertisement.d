module libsweatyballs.link.message.types.advertisement;

import libsweatyballs.link.message.core : Message;

/**
* Advertisement
*
* Description: This message type represents a router advertisement which contains information
* about its routes it is aware of
*/
public final class Advertisement : Message
{
    /* TODO: Fix this (this is just to get compilation to work) */
    this()
    {
        super(new byte[1]);
    }

    /* TODO: Fix this (this is just to get compilation to work) */
    this()
    {
        super("");
    }
}